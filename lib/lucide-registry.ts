// This registry provides a single, stable point for Lucide icon imports
// to resolve persistent IDE module resolution errors without affecting the build.

import * as Lucide from 'lucide-react';

export { Lucide };
export default Lucide;
